<div class="col-lg-2 text-center">
    <p class="quote">Search by categories</p>
  <div>
    <%= link_to "ALL", root_path, remote:true,class:"links border m-3" %>
    <%@categories.each do |categorie| %>
     <%=link_to categorie.name,root_path(categorie:categorie.name),remote:true ,class:"links border m-3"%>
    <%end%>
  </div>
</div>
